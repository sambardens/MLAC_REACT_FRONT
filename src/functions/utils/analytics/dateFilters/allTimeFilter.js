function allTimeFilter(selectedDateType) {
  const days = null;
  const month = null;
  const year = null;
  const preparedFilter = {
    selectedDateType, days, month, year, timeItem: 'year',
  };

  return preparedFilter;
}

export default allTimeFilter;
